name: generative_ui
description: Generative UI client-side orchestration with mock-first proof of concept for LLM integration.
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  alchemist: ^0.7.0

flutter:
  assets:
    - assets/ai_config.json
