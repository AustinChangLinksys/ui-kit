name: genui_example
description: Example app demonstrating GenUI Phase 3 - AWS Integration & Complete Client
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  generative_ui:
    path: ../
  ui_kit_library:
    path: ../../
  flutter_dotenv: ^5.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/.env
